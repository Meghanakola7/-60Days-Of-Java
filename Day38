Leetcode Practice:Q53,
----------------------------------------------------------------------------------------------------------------------------------------------------------------------
Array Manipulations:
(Q53):Find the maximum sub array sum
Code:
public class SubarrSum {
  public static void main(String args[]) {
    int maxsum=0;
    int[] arr={1,-2,6,-1,3};
    int n=arr.length;
    for(int i=0;i<=n-1;i++){
        int currentsum=0;
        for(int j=i;j<=n-1;j++){
            currentsum+=arr[j];
            if(maxsum<currentsum){
                maxsum=currentsum;
                
            }
            
        }
    }
    System.out.println("Maximum subarray:"+maxsum);
  }
}

---------------------------------------------------------------------------------------------------------------------------------------------------------------------
